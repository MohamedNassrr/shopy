import 'package:flutter/material.dart';

class DecorationViewBody extends StatelessWidget {
  const DecorationViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('decoration'),
      ],
    );
  }
}
